[package]
name = "clx-core"
version.workspace = true
edition = "2021"
description = "Shared native logic for clx (Tauri, GPUI, etc.)"
license.workspace = true

[dependencies]
enigo = "^0.6.1"

[target.'cfg(windows)'.dependencies]
is_elevated = "0.1"
